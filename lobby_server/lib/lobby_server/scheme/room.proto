syntax = "proto3";

message Room {
    int32 id = 1;
    string name = 2;
    string invite_code = 3;
    User owner = 4;
    // 유저 정보는 room에 유저가 입장하면 그떄 받아오는 걸로 한다.
    //repeated User members = 5;
}